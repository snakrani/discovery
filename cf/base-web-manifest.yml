---
inherit: base-manifest.yml

command: scripts/init-webserver.sh /dev/stderr && waitress-serve --expose-tracebacks --port=$PORT discovery.wsgi:application
memory: 128M
services:
  - discovery-config
  - discovery-db
  - discovery-tasks
  - discovery-auth