---
# all applications use these settings and services
memory: 1G
instances: 1
applications:
- name: discovery-staging
  path: .
  timeout: 180
buildpack: https://github.com/18F/python-buildpack.git#no-proto
services:
  - oasis
domain: 18f.gov
